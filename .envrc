use flake

# Create venv if missing, sync dependencies, and activate
if [ ! -d ".venv" ]; then
  echo "Creating Python venv..."
  uv venv
fi

# Sync dependencies (fast if already up to date)
uv sync --quiet 2>/dev/null || true

# Activate venv
source .venv/bin/activate
