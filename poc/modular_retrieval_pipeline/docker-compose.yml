version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: plm-redis-cache
    ports:
      - "6379:6379"
    volumes:
      - plm_redis_cache:/data
    command: redis-server --save 60 1 --loglevel warning

volumes:
  plm_redis_cache:
